# M4RV1N-Bot
Just a discord bot

## Setup
Create a virtual environment, install dependencies, then run the bot.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

```bash
python client.py
```
